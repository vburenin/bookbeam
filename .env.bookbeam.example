# Copy to .env.bookbeam and edit.
# Comma/space/semicolon separated user:pass pairs.
# Example: BOOKBEAM_USERS="user:password, foo:pass456"
BOOKBEAM_USERS="user:bookbeampass,foo:pass456"

# Set to 1 when served via HTTPS to mark cookies Secure.
# COOKIE_SECURE=1

# Absolute path to your audiobooks on the host machine (bind-mounted to /data in the container)
# Example:
# BOOKBEAM_DATA=/storage/audio-books
BOOKBEAM_DATA=/abs/path/to/your/audiobooks
